{
  "name": "kanban_app",
  "version": "0.0.1",
  "description": "example React/Webpack application.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack",
    "start": "webpack-dev-server --port 4000 --config webpack.development.js --devtool eval-source --progress --colors --hot --inline --history-api-fallback --content-base build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "css-loader": "^0.15.5",
    "node-libs-browser": "^0.5.2",
    "style-loader": "^0.12.3",
    "webpack": "^1.10.1",
    "webpack-dev-server": "^1.10.1"
  }
}
